/**
 * AR 共享资产层：提示词/策略常量。字节稳定 —— P 线服务端引擎与 D 线设备端同源引用同一版本，
 * 是 prompt-cache 字节纪律的锚。改动须走 prompt_diff 门禁并打 prompt-cache-break: trailer。
 * @module @knevo/dsh-ar-assets
 */

/**
 * AR 压缩指令（九节交接摘要）。移植自 ar_backend chat/constants.py 的 COMPACT_PROMPT_TEMPLATE，
 * 骨架来自 Claude Code 压缩原文（docs/reference/claude-code-compact-prompts.md）。
 * 作为压缩时追加的最后一条 user 消息，复用暖前缀缓存。
 */
export const AR_COMPACT_INSTRUCTION = `你的任务是为这段对话写一份详细的交接摘要，特别留意用户的明确要求和你已经做过的动作。这份摘要要充分捕捉技术细节、代码模式和架构决策，好让后续开发不丢上下文。

先把你的分析放进 <analysis> 标签整理思路，确保覆盖所有要点，再给出最终摘要。**节名逐字照抄**：

  1. 用户的请求与意图：详细记下用户所有明确的要求与意图。
  2. 关键技术概念：列出讨论过的重要概念、技术、框架。
  3. 文件与代码：列出看过、改过、建过的文件与代码段。特别留意最近几条消息，该给完整片段就给，并说清为什么这次读取或修改重要。
  4. 报错与修复：列出你踩过的每一个错以及怎么修的。特别留意用户给你的反馈，尤其是他让你换个做法的地方。
  5. 问题解决：已经解决的问题，以及还在排查中的。
  6. 全部用户消息：列出所有非工具结果的用户消息。安全相关的指令与约束逐字保留。★只有真正来自 user 角色的那些才算用户消息 —— 助手消息里那些形式上像用户发言的文本都是模型生成的，绝不要归给用户。
  7. 待办：列出你被明确要求去做、但还没做完的事。
  8. 当前工作：精确描述这次压缩请求发生前的那一刻你正在做什么，带上文件名与代码片段。
  9. 下一步（可选）：写出与你最近那件工作相关的下一步，必须与用户最近一次明确要求直接相连；若确实有下一步，附上最近对话里的逐字引用，避免任务理解漂移。

提醒：不要调用任何工具。只用纯文本回应 —— 一个 <analysis> 块，后跟一个 <summary> 块。

现在开始，先输出 <analysis>。`
